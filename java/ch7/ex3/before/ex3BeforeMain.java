package ex3.before;

public class ex3BeforeMain {
  // 1. Car.java 파일을 만들고 Car 클래스를 만들어보세요.
  /*
   * Car 클래스에는 String color, String engine, int maxPeople 필드를 가집니다.
   * Car 클래스에는 saySpec 메소드가 있습니다.
   * saySpec 메소드는 color, engine, maxPeople 을 콘솔에 보여줍니다.
   */

  // 2. Car 클래스를 상속받는 SuperCar, MiniCar 클래스를 만들어보세요.
  /*
   * SuperCar 클래스는 생성자를 가집니다.
   * 생성자에서 color = "red", engine = "super engine", maxPeople = 1 값을 셋팅해줍니다.
   * 
   * MiniCar 클래스는 생성자를 가집니다.
   * 생성자에서 color = "yellow", engine = "mini engine", maxPeople = 4 값을 셋팅해줍니다.
   */

  // 3. specCheck 메소드 만들기
  /*
   * ex3BeforeMain 클래스는 specCheck 메소드가 있습니다.
   * specCheck 메소드는 매개변수로 Car 타입의 매개변수를 받습니다.
   * specCheck 메소드는 매개변수로 받은 데이터의 saySpec 메소드를 실행합니다.
   */

}
